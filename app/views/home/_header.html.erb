<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to image_tag("logos/reciprocity-love-logo.png", style: 'max-height:50px'), home_index_url %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item">
          <a class="nav-link d-<%= request.original_fullpath == '/about' ? 'none' : 'block' %>" href="/about">About</a>
        </li>
        <li class="nav-item active">
          <a class="nav-link d-<%= request.original_fullpath == '/positions' ? 'none' : 'block' %>" href="/positions">Team</a>
        </li>
        <li class="nav-item active">
          <a class="nav-link <%= request.original_fullpath %> d-<%= request.original_fullpath === '/' || request.original_fullpath === '/welcome' ? 'none' : 'block' %>" href="/"> Dashboard</a>
        </li>
        <% end %>
    </ul>
    <ul class="navbar-nav my-2 my-lg-0">
      <% if user_signed_in? %>
        <li class="nav-item active">
          <%= link_to('Logout', destroy_user_session_path, method: :delete, class: "nav-link") %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
